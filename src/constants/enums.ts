export enum TypeToken {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  VerifyEmailToken
}

export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned
}

export enum MediaType {
  Image,
  Video
}
